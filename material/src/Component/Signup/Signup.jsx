import { Link } from 'react-router-dom';

const Signup = () => {

    const design = (
        <>
            <Link to={'/login'}>Login</Link>
        </>
    );
    return design;
}

export default Signup;